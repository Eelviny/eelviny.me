source 'https://rubygems.org'

gem 'jekyll'
group :jekyll_plugins do
  gem 'jekyll-sitemap' unless ENV["JEKYLL_ENV"] == 'development'
#  gem 'octopress-minify-html' unless ENV["JEKYLL_ENV"] == 'development'
end
